interface:
  display_name: "Omnigent"
  short_description: "Repo-specific patterns and workflows for omnigent"
  default_prompt: "Use the omnigent repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true